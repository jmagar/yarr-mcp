# Optional. Leave empty when the Rust binary serves the web UI and API from the same origin.
# Set this for `pnpm dev` when pointing the static UI at a separately running server.
NEXT_PUBLIC_RUSTARR_API_BASE_URL=
