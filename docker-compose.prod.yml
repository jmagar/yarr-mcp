# =============================================================================
# docker-compose.prod.yml — Production deployment for yarr-mcp
# =============================================================================

name: yarr

x-common-service: &common-service
  networks:
    - mcp
  restart: unless-stopped
  extra_hosts:
    - "host.docker.internal:host-gateway"
  ulimits:
    nofile:
      soft: 65535
      hard: 65535
  logging:
    driver: "json-file"
    options:
      max-size: "10m"
      max-file: "3"
      compress: "true"

services:
  yarr-mcp:
    <<: *common-service
    image: ghcr.io/jmagar/yarr:${YARR_MCP_VERSION:-latest}
    container_name: yarr-mcp
    env_file:
      - path: .env
        required: false
    environment:
      YARR_MCP_HOST: 0.0.0.0
      YARR_MCP_ALLOWED_HOSTS: "yarr.tootie.tv,localhost,127.0.0.1"
      YARR_MCP_ALLOWED_ORIGINS: "https://yarr.tootie.tv"
    ports:
      - "${YARR_MCP_HOST_PORT:-40070}:40070/tcp"
    volumes:
      - ${HOME}/.yarr:/data
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    user: "1000:1000"
    read_only: true
    tmpfs:
      - /tmp
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:40070/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.5'

networks:
  mcp:
    name: ${DOCKER_NETWORK:-mcp}
    external: true
