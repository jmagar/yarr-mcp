# Rustarr Live Endpoint Coverage

Generated by `cargo xtask live`.

Result: 127 live checks recorded in `target/live-full/report.json`; 127 passed, 0 failed.

Scope note: the "service API endpoint" column lists the endpoint families Rustarr allowlists and the concrete upstream endpoints Rustarr's curated actions use. It is not a complete inventory of every endpoint exposed by the upstream products.

## Sonarr

| Service API endpoint | Rustarr implementation | Live test |
|---|---|---|
| `/api/v3/system/status` | `service_status` + generic `api_get`; also the representative MCP transport round-trip through the `yarr` tool | Pass |
| `/api/v3/series` | generic `api_get` probe (matrix-backed) | Pass |
| `generated OpenAPI operations (op action)` | every spec operation dispatched and schema-validated by the `contract` suite | Pass |
| `/api/v3 allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes (CLI and MCP) | Pass |

## Radarr

| Service API endpoint | Rustarr implementation | Live test |
|---|---|---|
| `/api/v3/system/status` | `service_status` + generic `api_get` | Pass |
| `/api/v3/movie` | generic `api_get` probe (matrix-backed) | Pass |
| `generated OpenAPI operations (op action)` | every spec operation dispatched and schema-validated by the `contract` suite | Pass |
| `/api/v3 allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes | Pass |

## Prowlarr

| Service API endpoint | Rustarr implementation | Live test |
|---|---|---|
| `/api/v1/system/status` | `service_status` + generic `api_get` | Pass |
| `/api/v1/indexer` | generic `api_get` probe (matrix-backed) | Pass |
| `generated OpenAPI operations (op action)` | every spec operation dispatched and schema-validated by the `contract` suite | Pass |
| `/api/v1 allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes | Pass |

## Tautulli

| Service API endpoint | Rustarr implementation | Live test |
|---|---|---|
| `/api/v2?cmd=get_server_info` | `service_status` + generic `api_get` (query-API kind) | Pass |
| `/api/v2 allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes. Curated `stats_*` reads/maintenance are not yet re-homed on the live stack. | Pass |

## Overseerr

| Service API endpoint | Rustarr implementation | Live test |
|---|---|---|
| `/api/v1/status` | `service_status` + generic `api_get` | Pass |
| `generated OpenAPI operations (op action)` | every spec operation dispatched and schema-validated by the `contract` suite | Pass |
| `/api/v1 allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes | Pass |

## Bazarr

| Service API endpoint | Rustarr implementation | Live test |
|---|---|---|
| `/api/system/status` | `service_status` + generic `api_get` (GenericOnly kind) | Pass |
| `/api allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes. Seeded `api_delete` blacklist cleanup is not yet re-homed on the live stack. | Pass |

## Tracearr

| Service API endpoint | Rustarr implementation | Live test |
|---|---|---|
| `/health` | `service_status` + generic `api_get` (GenericOnly kind) | Pass |
| `/api/v1 allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes. Seeded `api_delete` debug-session cleanup is not yet re-homed on the live stack. | Pass |

## SABnzbd

| Service API endpoint | Rustarr implementation | Live test |
|---|---|---|
| `/api?mode=version&output=json` | `service_status` + generic `api_get` (query-API kind) | Pass |
| `/api?mode=queue&output=json` | generic `api_get` probe (matrix-backed) | Pass |
| `/api allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes. Curated `download_*` add/pause/resume/remove lifecycle is not yet re-homed on the live stack. | Pass |

## qBittorrent

| Service API endpoint | Rustarr implementation | Live test |
|---|---|---|
| `/api/v2/app/version` | `service_status` + generic `api_get` (cookie-auth kind) | Pass |
| `/api/v2/torrents/info` | generic `api_get` probe (matrix-backed) | Pass |
| `/api/v2 allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes. Curated `download_*` add/pause/resume/remove lifecycle is not yet re-homed on the live stack. | Pass |

## Plex

| Service API endpoint | Rustarr implementation | Live test |
|---|---|---|
| `/identity` | `service_status` + generic `api_get` (token-auth kind) | Pass |
| `generated OpenAPI operations (op action)` | every spec operation dispatched and schema-validated by the `contract` suite | Pass |
| `/identity, /library, /status, /servers allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes | Pass |

## Jellyfin

| Service API endpoint | Rustarr implementation | Live test |
|---|---|---|
| `/System/Info/Public` | `service_status` + generic `api_get` (token-auth kind) | Pass |
| `generated OpenAPI operations (op action)` | every spec operation dispatched and schema-validated by the `contract` suite | Pass |
| `/System, /Items, /Users, /Library, /Sessions allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes | Pass |

