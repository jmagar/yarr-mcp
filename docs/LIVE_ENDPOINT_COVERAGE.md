# Yarr Live Endpoint Coverage

Generated by `cargo xtask live`.

Result: 2 live checks recorded in `target/live-full/report.json`; 1 passed, 1 failed.

Scope note: the "service API endpoint" column lists the endpoint families Yarr allowlists and the concrete upstream endpoints Yarr's curated actions use. It is not a complete inventory of every endpoint exposed by the upstream products.

## Sonarr

| Service API endpoint | Yarr implementation | Live test |
|---|---|---|
| `/api/v3/system/status` | `service_status` + generic `api_get`; also the representative MCP transport round-trip through the `yarr` tool | Missing check: `cli status sonarr`, `service_status sonarr`, `api_get sonarr /api/v3/system/status`, `cli get sonarr /api/v3/system/status`, `mcp yarr service_status sonarr` |
| `/api/v3/series` | generic `api_get` probe (matrix-backed) | Missing check: `api_get sonarr /api/v3/series`, `cli get sonarr /api/v3/series` |
| `generated OpenAPI operations (op action)` | every spec operation dispatched and schema-validated by the `contract` suite | Fail |
| `/api/v3 allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes (CLI and MCP) | Missing check: `api_post unconfirmed upstream error sonarr`, `api_post confirmed upstream error sonarr`, `cli post unconfirmed upstream error sonarr`, `mcp api_post confirmed upstream error` |

## Radarr

| Service API endpoint | Yarr implementation | Live test |
|---|---|---|
| `/api/v3/system/status` | `service_status` + generic `api_get` | Missing check: `cli status radarr`, `service_status radarr`, `api_get radarr /api/v3/system/status`, `cli get radarr /api/v3/system/status` |
| `/api/v3/movie` | generic `api_get` probe (matrix-backed) | Missing check: `api_get radarr /api/v3/movie`, `cli get radarr /api/v3/movie` |
| `generated OpenAPI operations (op action)` | every spec operation dispatched and schema-validated by the `contract` suite | Missing check: `contract radarr` |
| `/api/v3 allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes | Missing check: `api_post unconfirmed upstream error radarr`, `api_post confirmed upstream error radarr`, `cli post unconfirmed upstream error radarr` |

## Prowlarr

| Service API endpoint | Yarr implementation | Live test |
|---|---|---|
| `/api/v1/system/status` | `service_status` + generic `api_get` | Missing check: `cli status prowlarr`, `service_status prowlarr`, `api_get prowlarr /api/v1/system/status`, `cli get prowlarr /api/v1/system/status` |
| `/api/v1/indexer` | generic `api_get` probe (matrix-backed) | Missing check: `api_get prowlarr /api/v1/indexer`, `cli get prowlarr /api/v1/indexer` |
| `generated OpenAPI operations (op action)` | every spec operation dispatched and schema-validated by the `contract` suite | Missing check: `contract prowlarr` |
| `/api/v1 allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes | Missing check: `api_post unconfirmed upstream error prowlarr`, `api_post confirmed upstream error prowlarr`, `cli post unconfirmed upstream error prowlarr` |

## Tautulli

| Service API endpoint | Yarr implementation | Live test |
|---|---|---|
| `/api/v2?cmd=get_server_info` | `service_status` + generic `api_get` (query-API kind) | Missing check: `cli status tautulli`, `service_status tautulli`, `api_get tautulli /api/v2?cmd=get_server_info`, `cli get tautulli /api/v2?cmd=get_server_info` |
| `/api/v2 allowlist (generic passthrough) + stats_* maintenance` | `api_post`/`api_put`/`api_delete` generic passthrough (unconfirmed + confirmed upstream-error probes); curated `stats_*` maintenance (refresh-libraries/refresh-users/delete-image-cache) exercised by the lifecycles suite | Missing check: `api_post unconfirmed upstream error tautulli`, `api_post confirmed upstream error tautulli`, `cli post unconfirmed upstream error tautulli`, `lifecycle tautulli maintenance` |

## Overseerr

| Service API endpoint | Yarr implementation | Live test |
|---|---|---|
| `/api/v1/status` | `service_status` + generic `api_get` | Missing check: `cli status overseerr`, `service_status overseerr`, `api_get overseerr /api/v1/status`, `cli get overseerr /api/v1/status` |
| `generated OpenAPI operations (op action)` | every spec operation dispatched and schema-validated by the `contract` suite | Missing check: `contract overseerr` |
| `/api/v1 allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes | Missing check: `api_post unconfirmed upstream error overseerr`, `api_post confirmed upstream error overseerr`, `cli post unconfirmed upstream error overseerr` |

## Bazarr

| Service API endpoint | Yarr implementation | Live test |
|---|---|---|
| `/api/system/status` | `service_status` + generic `api_get` (Subtitles capability) | Missing check: `cli status bazarr`, `service_status bazarr`, `api_get bazarr /api/system/status`, `cli get bazarr /api/system/status` |
| `/api allowlist (generic passthrough) + seeded blacklist delete` | `api_post`/`api_put`/`api_delete` generic passthrough (unconfirmed + confirmed upstream-error probes); seeded `api_delete` movie-blacklist cleanup exercised by the lifecycles suite | Missing check: `api_post unconfirmed upstream error bazarr`, `api_post confirmed upstream error bazarr`, `cli post unconfirmed upstream error bazarr`, `lifecycle bazarr blacklist delete` |

## Tracearr

| Service API endpoint | Yarr implementation | Live test |
|---|---|---|
| `/health` | `service_status` + generic `api_get` (Trace capability) | Missing check: `cli status tracearr`, `service_status tracearr`, `api_get tracearr /health`, `cli get tracearr /health` |
| `/api/v1 allowlist (generic passthrough) + seeded debug-session delete` | `api_post`/`api_put`/`api_delete` generic passthrough (unconfirmed + confirmed upstream-error probes); seeded `api_delete` debug-session cleanup exercised by the lifecycles suite | Missing check: `api_post unconfirmed upstream error tracearr`, `api_post confirmed upstream error tracearr`, `cli post unconfirmed upstream error tracearr`, `lifecycle tracearr debug delete` |

## SABnzbd

| Service API endpoint | Yarr implementation | Live test |
|---|---|---|
| `/api?mode=version&output=json` | `service_status` + generic `api_get` (query-API kind) | Missing check: `cli status sabnzbd`, `service_status sabnzbd`, `api_get sabnzbd /api?mode=version&output=json`, `cli get sabnzbd /api?mode=version&output=json` |
| `/api?mode=queue&output=json` | generic `api_get` probe (matrix-backed) | Missing check: `api_get sabnzbd /api?mode=queue&output=json`, `cli get sabnzbd /api?mode=queue&output=json` |
| `/api allowlist (generic passthrough) + download_* lifecycle` | `api_post`/`api_put`/`api_delete` generic passthrough (unconfirmed + confirmed upstream-error probes); curated `download_*` add/pause/resume/remove lifecycle exercised against a fixture NZB by the lifecycles suite | Missing check: `api_post unconfirmed upstream error sabnzbd`, `api_post confirmed upstream error sabnzbd`, `cli post unconfirmed upstream error sabnzbd`, `lifecycle sabnzbd download` |

## qBittorrent

| Service API endpoint | Yarr implementation | Live test |
|---|---|---|
| `/api/v2/app/version` | `service_status` + generic `api_get` (cookie-auth kind) | Missing check: `cli status qbittorrent`, `service_status qbittorrent`, `api_get qbittorrent /api/v2/app/version`, `cli get qbittorrent /api/v2/app/version` |
| `/api/v2/torrents/info` | generic `api_get` probe (matrix-backed) | Missing check: `api_get qbittorrent /api/v2/torrents/info`, `cli get qbittorrent /api/v2/torrents/info` |
| `/api/v2 allowlist (generic passthrough) + download_* lifecycle` | `api_post`/`api_put`/`api_delete` generic passthrough (unconfirmed + confirmed upstream-error probes); curated `download_*` add/pause/resume/remove lifecycle exercised against a test magnet by the lifecycles suite | Missing check: `api_post unconfirmed upstream error qbittorrent`, `api_post confirmed upstream error qbittorrent`, `cli post unconfirmed upstream error qbittorrent`, `lifecycle qbittorrent download` |

## Plex

| Service API endpoint | Yarr implementation | Live test |
|---|---|---|
| `/identity` | `service_status` + generic `api_get` (token-auth kind) | Missing check: `cli status plex`, `service_status plex`, `api_get plex /identity`, `cli get plex /identity` |
| `generated OpenAPI operations (op action)` | every spec operation dispatched and schema-validated by the `contract` suite | Missing check: `contract plex` |
| `/identity, /library, /status, /servers allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes | Missing check: `api_post unconfirmed upstream error plex`, `api_post confirmed upstream error plex`, `cli post unconfirmed upstream error plex` |

## Jellyfin

| Service API endpoint | Yarr implementation | Live test |
|---|---|---|
| `/System/Info/Public` | `service_status` + generic `api_get` (token-auth kind) | Missing check: `cli status jellyfin`, `service_status jellyfin`, `api_get jellyfin /System/Info/Public`, `cli get jellyfin /System/Info/Public` |
| `generated OpenAPI operations (op action)` | every spec operation dispatched and schema-validated by the `contract` suite | Missing check: `contract jellyfin` |
| `/System, /Items, /Users, /Library, /Sessions allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes | Missing check: `api_post unconfirmed upstream error jellyfin`, `api_post confirmed upstream error jellyfin`, `cli post unconfirmed upstream error jellyfin` |

