# Yarr Live Endpoint Coverage

Generated by `cargo xtask live`.

Result: 132 live checks recorded in `target/live-full/report.json`; 126 passed, 6 failed.

Scope note: the "service API endpoint" column lists the endpoint families Yarr allowlists and the concrete upstream endpoints Yarr's curated actions use. It is not a complete inventory of every endpoint exposed by the upstream products.

## Sonarr

| Service API endpoint | Yarr implementation | Live test |
|---|---|---|
| `/api/v3/system/status` | `service_status` + generic `api_get`; also the representative MCP transport round-trip through the `yarr` tool | Pass |
| `/api/v3/series` | generic `api_get` probe (matrix-backed) | Pass |
| `generated OpenAPI operations (op action)` | every spec operation dispatched and schema-validated by the `contract` suite | Fail |
| `/api/v3 allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes (CLI and MCP) | Pass |

## Radarr

| Service API endpoint | Yarr implementation | Live test |
|---|---|---|
| `/api/v3/system/status` | `service_status` + generic `api_get` | Pass |
| `/api/v3/movie` | generic `api_get` probe (matrix-backed) | Pass |
| `generated OpenAPI operations (op action)` | every spec operation dispatched and schema-validated by the `contract` suite | Fail |
| `/api/v3 allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes | Pass |

## Prowlarr

| Service API endpoint | Yarr implementation | Live test |
|---|---|---|
| `/api/v1/system/status` | `service_status` + generic `api_get` | Pass |
| `/api/v1/indexer` | generic `api_get` probe (matrix-backed) | Pass |
| `generated OpenAPI operations (op action)` | every spec operation dispatched and schema-validated by the `contract` suite | Fail |
| `/api/v1 allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes | Pass |

## Tautulli

| Service API endpoint | Yarr implementation | Live test |
|---|---|---|
| `/api/v2?cmd=get_server_info` | `service_status` + generic `api_get` (query-API kind) | Pass |
| `/api/v2 allowlist (generic passthrough) + stats_* maintenance` | `api_post`/`api_put`/`api_delete` generic passthrough (unconfirmed + confirmed upstream-error probes); curated `stats_*` maintenance (refresh-libraries/refresh-users/delete-image-cache) exercised by the lifecycles suite | Pass |

## Overseerr

| Service API endpoint | Yarr implementation | Live test |
|---|---|---|
| `/api/v1/status` | `service_status` + generic `api_get` | Pass |
| `generated OpenAPI operations (op action)` | every spec operation dispatched and schema-validated by the `contract` suite | Fail |
| `/api/v1 allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes | Pass |

## Bazarr

| Service API endpoint | Yarr implementation | Live test |
|---|---|---|
| `/api/system/status` | `service_status` + generic `api_get` (GenericOnly kind) | Pass |
| `/api allowlist (generic passthrough) + seeded blacklist delete` | `api_post`/`api_put`/`api_delete` generic passthrough (unconfirmed + confirmed upstream-error probes); seeded `api_delete` movie-blacklist cleanup exercised by the lifecycles suite | Pass |

## Tracearr

| Service API endpoint | Yarr implementation | Live test |
|---|---|---|
| `/health` | `service_status` + generic `api_get` (GenericOnly kind) | Pass |
| `/api/v1 allowlist (generic passthrough) + seeded debug-session delete` | `api_post`/`api_put`/`api_delete` generic passthrough (unconfirmed + confirmed upstream-error probes); seeded `api_delete` debug-session cleanup exercised by the lifecycles suite | Pass |

## SABnzbd

| Service API endpoint | Yarr implementation | Live test |
|---|---|---|
| `/api?mode=version&output=json` | `service_status` + generic `api_get` (query-API kind) | Pass |
| `/api?mode=queue&output=json` | generic `api_get` probe (matrix-backed) | Pass |
| `/api allowlist (generic passthrough) + download_* lifecycle` | `api_post`/`api_put`/`api_delete` generic passthrough (unconfirmed + confirmed upstream-error probes); curated `download_*` add/pause/resume/remove lifecycle exercised against a fixture NZB by the lifecycles suite | Pass |

## qBittorrent

| Service API endpoint | Yarr implementation | Live test |
|---|---|---|
| `/api/v2/app/version` | `service_status` + generic `api_get` (cookie-auth kind) | Pass |
| `/api/v2/torrents/info` | generic `api_get` probe (matrix-backed) | Pass |
| `/api/v2 allowlist (generic passthrough) + download_* lifecycle` | `api_post`/`api_put`/`api_delete` generic passthrough (unconfirmed + confirmed upstream-error probes); curated `download_*` add/pause/resume/remove lifecycle exercised against a test magnet by the lifecycles suite | Pass |

## Plex

| Service API endpoint | Yarr implementation | Live test |
|---|---|---|
| `/identity` | `service_status` + generic `api_get` (token-auth kind) | Pass |
| `generated OpenAPI operations (op action)` | every spec operation dispatched and schema-validated by the `contract` suite | Fail |
| `/identity, /library, /status, /servers allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes | Pass |

## Jellyfin

| Service API endpoint | Yarr implementation | Live test |
|---|---|---|
| `/System/Info/Public` | `service_status` + generic `api_get` (token-auth kind) | Pass |
| `generated OpenAPI operations (op action)` | every spec operation dispatched and schema-validated by the `contract` suite | Fail |
| `/System, /Items, /Users, /Library, /Sessions allowlist (generic passthrough)` | `api_post`/`api_put`/`api_delete` generic passthrough; unconfirmed + confirmed upstream-error probes | Pass |

