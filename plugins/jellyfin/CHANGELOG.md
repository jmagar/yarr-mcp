# Changelog

All notable changes to the Jellyfin plugin are documented here.

## [Unreleased]

### Added
- Initial standalone Jellyfin skills-only plugin, split out of the bundled `arrs` plugin.
- Isolated credential bridge writing to `~/.config/lab-<service>/config.json`.
