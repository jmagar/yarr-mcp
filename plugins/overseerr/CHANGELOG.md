# Changelog

All notable changes to the Overseerr plugin are documented here.

## [Unreleased]

### Added
- Initial standalone Overseerr skills-only plugin, split out of the bundled `arrs` plugin.
- Isolated credential bridge writing to `~/.config/lab-<service>/config.json`.
