# Changelog

All notable changes to the Sonarr plugin are documented here.

## [Unreleased]

### Added
- Initial standalone Sonarr skills-only plugin, split out of the bundled `arrs` plugin.
- Isolated credential bridge writing to `~/.config/lab-<service>/config.env`.
