# Changelog

All notable changes to the Tautulli plugin are documented here.

## [Unreleased]

### Added
- Initial standalone Tautulli skills-only plugin, split out of the bundled `arrs` plugin.
- Isolated credential bridge writing to `~/.config/lab-<service>/config.env`.
