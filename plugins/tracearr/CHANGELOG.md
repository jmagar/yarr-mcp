# Changelog

All notable changes to the Tracearr plugin are documented here.

## [Unreleased]

### Added
- Initial standalone Tracearr skills-only plugin, split out of the bundled `arrs` plugin.
- Isolated credential bridge writing to `~/.config/lab-<service>/config.env`.
