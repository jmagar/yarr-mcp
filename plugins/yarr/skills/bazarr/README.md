# Bazarr skill

Manage subtitles in Bazarr via its REST API. See `SKILL.md` for commands.
Credentials come from the bazarr plugin settings, bridged to
`~/.config/lab-bazarr/config.env` by the plugin's SessionStart hook.
