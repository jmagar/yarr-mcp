# Repo-relative paths or glob patterns allowed to exceed the default blob limit.
# Keep this list short. Prefer Git LFS or generated artifacts outside git.
#
# Rustarrs:
# bin/rustarr
# docs/references/**/*.md

# Vendored upstream OpenAPI specs — the SSOT for `cargo xtask gen-openapi`. These
# are authoritative third-party documents (Jellyfin ~1.8 MiB, Plex ~650 KiB), not
# hand-written source; they intentionally live in git so codegen is reproducible.
specs/*
